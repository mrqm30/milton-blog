---
import Base from '../layouts/Base.astro';
import Section from '../components/Section.astro';
import Tag from '../components/Tag.astro';
const skills = {
  "Lenguajes": ["Python", "R", "SQL", "C++", "JavaScript"],
  "Data/ML": ["Pandas/Polars", "PySpark", "Scikit-learn", "Data Vault 2.0"],
  "Web": ["Astro", "Tailwind", "HTML", "CSS"],
  "DevOps": ["Git/GitHub", "Linux", "Docker"]
};
---
<Base title="Habilidades — Milton QM">
  {Object.entries(skills).map(([cat, list]) => (
    <Section title={cat}>
      <div class="flex flex-wrap gap-2">
        {list.map(s => <Tag text={s} />)}
      </div>
    </Section>
  ))}
</Base>
